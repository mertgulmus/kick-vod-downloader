# Kick VOD Downloader - Environment Configuration
# Copy this file to .env and customize the values
#
# Usage:
#   1. Copy: cp env.example .env  (or on Windows: copy env.example .env)
#   2. Edit the .env file with your settings
#   3. Run: docker-compose up -d

# Required: Comma-separated list of Kick channel names to monitor
# Examples:
#   Single channel: CHANNELS=channelname
#   Multiple channels: CHANNELS=channel1,channel2,channel3
CHANNELS=

# Preferred video quality (e.g., 480p30, 720p30, 1080p30)
QUALITY=480p30

# Polling interval in seconds for checking new segments during streaming
POLL_SECONDS=60

# Interval in seconds for checking if channels are live
LIVE_CHECK_SECONDS=60

# Local path where downloads will be saved
# This is the host path that will be mounted to /downloads in the container
# Use absolute path or relative path from this directory
# Windows example: DOWNLOAD_PATH=C:/Users/YourName/Downloads/kick-vods
# Linux/Mac example: DOWNLOAD_PATH=/home/yourname/kick-vods
DOWNLOAD_PATH=./kick_vod_downloads

# Enable HTTP request debugging (true/false)
DEBUG_HTTP=false

# Enable verbose debug logging (true/false)
DEBUG_VERBOSE=false
